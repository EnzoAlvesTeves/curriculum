spring:
  application:
    name: curriculum
  datasource:
    url: jdbc:mysql://192.168.0.7:3306/curriculum
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

